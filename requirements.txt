# ── Astra v2 core ─────────────────────────────────────────────────────────────
pandas>=2.0.0
numpy>=1.26.0
pyarrow>=14.0.0          # parquet cache for FRED/Dukascopy data
requests>=2.31.0
yfinance>=0.2.36
python-dateutil>=2.8.2
APScheduler>=3.10.4

# ── AI / LLM ─────────────────────────────────────────────────────────────────
google-genai>=1.0.0          # new Gemini SDK (replaces google-generativeai)
# google-generativeai>=0.7.0  # legacy, kept as fallback if google-genai unavailable

# ── Storage ───────────────────────────────────────────────────────────────────
python-dotenv>=1.0.0

# ── Notifications ─────────────────────────────────────────────────────────────
pyTelegramBotAPI>=4.14.0

# ── Full-auto trading (Windows VPS only) ─────────────────────────────────────
# MetaTrader5>=5.0.45

# ── Testing ───────────────────────────────────────────────────────────────────
pytest>=8.0.0
pytest-cov>=5.0.0

# ── Legacy v1 deps (kept for existing code, remove when v1 retired) ───────────
Flask>=3.0.0
flask-cors>=4.0.0
gnews>=0.3.7
beautifulsoup4>=4.12.0
curl_cffi>=0.6.0
plotly>=5.18.0
kaleido>=1.2.0
Pillow>=10.0.0
